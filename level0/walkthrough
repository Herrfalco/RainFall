- atoi(argv[1]) needs to be equal to 423 in order to enter first if condition
- execv then runs "/bin/sh"
	- ./level0 423
- thanks to the setuid access right we can now access level1 flag
- inside new shell
	- cat /home/user/level1/.pass
